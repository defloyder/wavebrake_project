package bridge

// gomobile's generated glue code imports this package at build time; a
// plain `go get` without a real import gets pruned straight back out by
// `go mod tidy` since nothing in this module otherwise references it.
import (
	_ "github.com/sagernet/gomobile/bind"
)
