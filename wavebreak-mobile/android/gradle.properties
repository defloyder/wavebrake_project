org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# flutter_v2ray (Xray-core's bundled .so libraries) needs its native libs
# kept uncompressed at runtime. The AGP version this project uses removed
# the `android.bundle.enableUncompressedNativeLibs` property entirely
# (uncompressed is now the only behavior), so nothing to set here.
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
